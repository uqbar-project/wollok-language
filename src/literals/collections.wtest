describe "collections"{
		
	test "Empty list"{
		assert.equals([],[])
	}
	
	test "Non-empty list"{
		assert.equals([1,2,3], [1,2,3])
	}
	
	test "Empty set"{
		assert.equals(#{}, #{})
	}
	
	test "Non-empty set"{
		assert.equals(#{1,2,3}, #{1,2,3})
	}
	
}