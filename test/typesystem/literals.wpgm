class C {
	@Expect(type="String")
	const property unString = "Hola" 

	@Expect(type="Number")
	const property largo = 1

	@Expect(type="List<Any>")
	const property coleccion = [] 

	@Expect(type="Any")
	const property algo
	
	// TOIMPLEMENT
	// @Expect(type="{  }")
	const property obj = object { }

	@Expect(type="Any")
	const property nullable = null
}

