program p {	
	@Expect(type="Object")
	var comparable
	
	if (comparable == @Expect(type="Any") null) {
		comparable = @Expect(type="Object") null
	}
	
	@Expect(type="Number")
	var num1 = 1
	
	@Expect(type="Number")
	num1 = null
}

