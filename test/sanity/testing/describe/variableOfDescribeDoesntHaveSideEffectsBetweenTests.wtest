describe "pruebas generales" {
	var one = 1
			
	method incrementOne() {
		one = one + 1
	}
			
	method one() = 1
			
	test "Dos es one + 1" {
		self.incrementOne()
		assert.equals(one, self.one() + 1)
	}
			
	test "Uno es one" {
		assert.equals(one, self.one())
	}
}