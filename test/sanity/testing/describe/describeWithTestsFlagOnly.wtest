describe "describe - test marked with only flag" {
  only test "this test shall pass" {
    assert.that(true)
  }
  
  test "while this test should not execute" {
    assert.fail("test should not execute")
  }
}
